# Four.gw Docs

# Docs: Update 8.0.2 

Code updated `four.js` since last Friday. Check the new `/four-docs` folder for docs. Check `code-fixes` to see the `.md` files with errors, and the `.html` files with errors and fixes within that folder. The other folder inside `fixes` will also contain the fixes. 
